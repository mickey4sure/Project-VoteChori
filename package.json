{
  "name": "arena-elections",
  "version": "1.0.0",
  "description": "Election Process Education platform",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "dev": "pnpm --filter frontend dev & pnpm --filter backend dev",
    "migrate": "pnpm --filter backend exec prisma migrate dev",
    "migrate:deploy": "pnpm --filter backend exec prisma migrate deploy",
    "db:seed": "pnpm --filter backend exec tsx prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
